# Architecture

## Structure

- `apps/mobile`: React Native Expo client.
- `apps/api`: FastAPI backend.
- `packages/shared`: shared types and interface definitions.
- `docs`: product, architecture, and API documentation.

## Runtime

The mobile app communicates with the FastAPI backend over HTTP APIs.

